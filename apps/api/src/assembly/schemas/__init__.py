"""Pydantic schemas. The API uses these for request validation and response
serialization. The simulation pipeline uses these for typed structured-output
calls into the LLM provider abstraction (Phase 3+)."""
